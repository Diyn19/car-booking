services:
  - type: web
    name: car-booking
    env: python
  pythonVersion: 3.10
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: FLASK_ENV
        value: production
      - key: EMAIL_PASSWORD
        value: your_actual_email_password_here
